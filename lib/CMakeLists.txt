SET(gp_lib_src
	MatrixIO.cpp
)
ADD_LIBRARY(gplib SHARED ${gp_lib_src})

SET_TARGET_PROPERTIES( gplib
	PROPERTIES
	VERSION 0.0.1
	SOVERSION 0.0.1)

INSTALL(TARGETS
	gplib
DESTINATION
	lib
PERMISSIONS
	OWNER_READ GROUP_READ WORLD_READ
)
